## Stack
Ruby (version unspecified); Bundler for dependency management; Docker for containerisation.

## Constraints
Never modify:
- `Gemfile.lock` (managed by bundle update tooling only)
- `Dockerfile` (managed by dockerfile_lint tooling only)
- Any credential or secrets files (`.env`, `*.key`, `*.pem`, `config/credentials.yml.enc`)
- Any migration files under `db/migrate/`
- Generated or vendored code under `vendor/`

## Conventions
- Repository contains 16 tracked files; most logic is non-Ruby (Ruby is 6%), so changes outside `.rb` files are likely infrastructure or config.
- Follow existing Ruby file naming: snake_case for files and classes.
- Tests (if present) live alongside source files or in a `spec/` or `test/` directory; match existing pattern before adding new test files.
- Docker image is the primary deployment artefact; keep `Dockerfile` changes to the designated tooling skill.

## Dependency manifests
- `Gemfile` — declare Ruby gem dependencies here; do not pin versions unless required.
- `Gemfile.lock` — never edit by hand; regenerated by `bundle install` / `bundle update`.
